#ifndef STAFFDEMO_USAGES_H
#define STAFFDEMO_USAGES_H
#include "string"

using Message           = std::string;
using TextLabel         = std::string;
using Text              = std::string;
using Directory         = std::string;
using Translate         = std::string;
using Language          = std::string;
using Key               = std::string;

using Size              = std::size_t;

using Check             = bool;

using ColorID           = short;
using ColorType         = std::string;

using Counter           = std::size_t;
using gaugeCounter      = float;

using ScreenID          = short;



#endif //STAFFDEMO_USAGES_H
